auto-install-peers=true
strict-peer-dependencies=false

# Shared morethan web-workbench kit, published to GitHub Packages.
# Auth token lives in the global ~/.npmrc (not committed).
@willyu1007:registry=https://npm.pkg.github.com
