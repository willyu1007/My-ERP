/**
 * @my-erp/api-client — TypeScript client generated from the OpenAPI contract
 * (docs/context/api/openapi.yaml). Generation is wired in once REST endpoints
 * land (P3+); P0a ships the package boundary only.
 */
export const API_CLIENT_PACKAGE = '@my-erp/api-client' as const;
